// This file exists only as a placeholder.  The current project uses Clerk for
// authentication, so `next-auth` isn't part of the dependencies and the
// types above would cause compiler errors.  If the project ever migrates to
// NextAuth the proper declarations can be added here.
